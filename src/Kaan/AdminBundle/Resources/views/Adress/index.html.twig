{% extends 'AdminBundle::layout.html.twig' %}
{% block content %}
<div class="page-header">
    <h1 class="pull-left">User Adres List</h1>
    <div class="pull-right" style="margin-top:15px">
    <a class="btn btn-large btn-primary" href="{{ path('adress_new') }}?id={{ app.request.get('id') }}">
        <i class="icon-white icon-plus"></i> Create
    </a>
    </div>
    <div class="clearfix"></div>
</div>
<table class="records_list table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Taşıma İsmi</th>
            <th style="width: 100px">Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.name }}</td>
            <td>{{ entity.shippingname }}</td>
            <td>
                <div class="btn-group">
                    <a class="btn" href="{{ path('adress_show', { 'id': entity.id }) }}">
                        <i class="icon-eye-open"></i> Show
                    </a>
                    <a class="btn btn-primary" href="{{ path('adress_edit', { 'id': entity.id }) }}?id={{ entity.user.id }}">
                        <i class="icon-white icon-pencil"></i> Edit
                    </a>
                </div>
            </td>
        </tr>
    {% else %}
        <h1>Adres Bulunamadı</h1>
    {% endfor %}
    </tbody>
</table>

{% endblock %}