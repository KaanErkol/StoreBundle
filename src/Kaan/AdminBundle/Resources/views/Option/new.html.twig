{% extends 'AdminBundle::layout.html.twig' %}
{% block content %}
<div class="page-header">
    <h1>Option Creation</h1>
</div>
{{ form_errors(form) }}
<form action="{{ path('option_create') }}" method="post" class="form-horizontal" {{ form_enctype(form) }}>
    {{ form_widget(form._token) }}  
    <div class="control-group">
        {{ form_label(form.name, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
        <div class="controls">
            {{ form_widget(form.name) }}
        </div>
    </div>
    <div class="control-group">
        {{ form_label(form.presentation, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
        <div class="controls">
            {{ form_widget(form.presentation) }}
        </div>
    </div>

    <div class="control-group">
        <div class="controls">
            <ul class="unstyled child" data-prototype="{{ form_widget(form.child.vars.prototype)|e }}">
                {% for val in form.child %}
                    <li style="margin-bottom: 5px;">Value :{{ form_widget(val.value) }}</li>
                {% endfor %}
            </ul>
        </div>
            <div class="clearfix"></div>
    </div>
  <div class="control-group">
    <div class="controls">
      <button type="submit" class="btn">Create</button>
    </div>
  </div>
</form>

<ul class="record_actions unstyled">
    <li>
        <a class="btn" href="{{ path('option') }}">
            <i class="icon-circle-arrow-left"></i> Back to the list
        </a>
    </li>
</ul>
{% endblock %}
{% block javascript %}
<script>
// Get the div that holds the collection of tags
var collectionHolder = $('ul.child');

// setup an "add a tag" link
var $addTagLink = $('<a href="#" class="add_tag_link btn btn-success" style="margin-top:10px">Add a Value</a>');
var $newLinkLi = $('<li></li>').append($addTagLink);

jQuery(document).ready(function() {
    // add a delete link to all of the existing tag form li elements
    collectionHolder.find('li').each(function() {
        addTagFormDeleteLink($(this));
    });
    // add the "add a tag" anchor and li to the tags ul
    collectionHolder.append($newLinkLi);

    // count the current form inputs we have (e.g. 2), use that as the new
    // index when inserting a new item (e.g. 2)
    collectionHolder.data('index', collectionHolder.find(':input').length);

    $addTagLink.on('click', function(e) {
        // prevent the link from creating a "#" on the URL
        e.preventDefault();

        // add a new tag form (see next code block)
        addTagForm(collectionHolder, $newLinkLi);
    });
    
});
function addTagForm(collectionHolder, $newLinkLi) {
    // Get the data-prototype explained earlier
    var prototype = collectionHolder.data('prototype');

    // get the new index
    var index = collectionHolder.data('index');

    // Replace '__name__' in the prototype's HTML to
    // instead be a number based on how many items we have
    var newForm = prototype.replace(/__name__/g, index);

    // increase the index with one for the next item
    collectionHolder.data('index', index + 1);

    // Display the form in the page in an li, before the "Add a tag" link li
    var $newFormLi = $('<li></li>').append(newForm);
    $newLinkLi.before($newFormLi);
    // add a delete link to the new form
    addTagFormDeleteLink($newFormLi);
}
function addTagFormDeleteLink($tagFormLi) {
    var $removeFormA = $('<a href="#" class="btn btn-danger btn-mini">X</a>');
    $tagFormLi.append($removeFormA);

    $removeFormA.on('click', function(e) {
        // prevent the link from creating a "#" on the URL
        e.preventDefault();

        // remove the li for the tag form
        $tagFormLi.remove();
    });
}

</script>
{% endblock %}